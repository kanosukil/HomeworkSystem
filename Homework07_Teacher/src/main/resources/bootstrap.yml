# 该服务的端口
server:
  port: 9997
# 服务注册名
spring:
  application:
    name: TeacherService
  # Nacos 配置
  cloud:
    nacos:
      server-addr: 127.0.0.1:8800
      config:
        group: Homework_System-Config
        name: teacher-config
        file-extension: yml