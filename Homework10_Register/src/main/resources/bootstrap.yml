# 该服务的端口
server:
  port: 10000
# 服务注册名
spring:
  application:
    name: RegisterService
  # Nacos 配置
  cloud:
    nacos:
      server-addr: 127.0.0.1:8800
      config:
        group: Homework_System-Config
        name: register-config
        file-extension: yml