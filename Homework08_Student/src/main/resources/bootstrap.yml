# 该服务的端口
server:
  port: 9998
# 服务注册名
spring:
  application:
    name: StudentService
  # Nacos 配置
  cloud:
    nacos:
      server-addr: 127.0.0.1:8800
      config:
        namespace: Homework-System
        group: Homework_System-Config
        name: student-config
        file-extension: yml